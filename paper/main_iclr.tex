%% ICLR 2027 submission — chemical-transformer expansion (v3: dropout-lead).
%%
%% POLICY CHECKLIST (executed by hand, not by LaTeX):
%%  [ ] Sep 16: freeze conservative abstract (below). Sep 17: register.
%%      Abstract editable until Sep 25 — sharpen as results land.
%%  [ ] OpenReview profile accuracy; authors frozen Sep 18; one-paper cap.
%%  [ ] 6-10 pages main text HARD. Appendix unlimited.
%%  [ ] Swap iclr2026_conference.sty -> official iclr2027 when published.
%%  [ ] PARITY TOGGLE: decided by the 10k convergence pair — see \ifparity.
\documentclass{article}
\usepackage{iclr2027_conference}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\input{results_iclr}   % auto-generated macros + table bodies

\iclrfinalcopy

\title{Endogenous Signals Fail to Allocate: \\
Per-Token Compute Value Is Real, \\
Transient, and Not Learnable}

\author{Anonymous authors\\
Paper under double-blind review}

\newcommand{\LN}{\mathrm{LN}}
\newcommand{\FFN}{\mathrm{FFN}}
\newcommand{\attn}{\mathrm{attn}}
% PARITY TOGGLE (decided Sep 15 by the 10k convergence pair): rotation held
% loss 1.086 vs dense 1.032 = 5.3% gap at 50% billed FLOPs -> inside the
% pre-registered 2-10% band -> claim SCOPED to compute-limited regimes.
\newif\ifparity
\parityfalse
\newcommand{\XX}{\textbf{\color{red}??}}
\newcommand{\todoX}[1]{\textcolor{red}{[X: #1]}}

\begin{document}
\maketitle

\begin{abstract}
Token-level adaptive compute usually keys off endogenous signals---attention
entropy, router scores, confidence---assumed to track per-token difficulty.
On arithmetic with graded difficulty we show this assumption fails three
ways: an entropy-driven gate collapses to a constant, a tag-initialized
gate inverts, and an unsupervised learned head is vacuous.
Difficulty-supervised targets under a budget constraint do produce ordered
gates, but a pre-registered shuffled-targets control shows the ordering
carries no accuracy benefit: gate-grad targets trail permuted targets by
0.0008 held-out loss at 1.4B, and by at most 0.3 accuracy points at 11M.
Measuring the field directly---the per-token gradient of loss with respect
to each token's compute gate---explains why. The field has a life cycle:
real structure mid-training (permutation-controlled oracle), consumed by
training, flat at convergence---and a causal test shows its structure is
not trainable: chasing it online loses to random allocation of the same
budget at every scale tested. The surviving recipe needs no allocator:
token-level FFN dropout, where each token receives full FFN compute only
inside randomly rotating windows. At 1.4B web-text fine-tuning it trails
dense by 7.8\% held-out loss (2 seeds) at 50\% billed FFN training FLOPs,
inside the pre-registered 2--10\% band; a gather-based realization
converts half the billed cost into a measured 1.35$\times$ training
throughput. Failures on code and template-dominated math are reported,
not hidden.
\end{abstract}

% ---- ACTIVE INTRO (dropout-lead; parity-conditional sentence marked) ----
Transformers spend most of their training compute on feed-forward blocks
applied uniformly to every token at every step. We ask a simple question:
does every token need the FFN at every step? The adaptive-compute literature
answers with learned allocators---routers, entropy gates, difficulty
heads---trained end-to-end to switch compute on where it ``matters.'' We
first show this program fails on its own terms in a controlled testbed with
per-token ground truth: endogenous signals collapse, invert, or stay vacuous
(\S\ref{sec:failures}), supervised targets produce correctly ordered gates
whose ordering carries no accuracy benefit, and a pre-registered
shuffled-targets control shows target content is irrelevant at 11M and
150M scale.
The positive half of the paper is a measurement that explains these
failures and a recipe it motivates. We measure the per-token
\emph{value of compute}---the gradient of the loss with respect to each
token's FFN gate---and find it has a life cycle: permutation-controlled
measurement shows real structure mid-training, consumed by training and
flat at convergence (\S\ref{sec:measure}). A permutation-controlled oracle
bounds the converged field's realizable headroom, and a causal single-stage
test shows that \emph{chasing} the field online is worse than random
allocation of the same budget---at 11M and, replicated, in the starved-150M
regime where the field's mid-life structure is demonstrably present
(\S\ref{sec:results}). What works is the degenerate allocator everyone
skips: train with \emph{token-level FFN dropout}---each token receives full
FFN compute only inside randomly rotating windows---and deploy dense. At
matched tokens this trails dense held-out loss by \ConvRotGapPct\% at
half the billed FFN training FLOPs on 11M and 150M models, and by
\NlRotGapPct\% on 1.4B web-text fine-tuning---a claim we scope to
compute-limited regimes per its pre-registered decision rule---requires no
labels or routers, and composes with any attention architecture because it
never touches attention.

\paragraph{Contributions.}
\begin{enumerate}
\item \textbf{A three-way failure diagnosis} of endogenous allocation signals
(collapse, inversion, vacuity), each with a mechanistic account
(\S\ref{sec:failures}); a standard Mixture-of-Depths router fails
analogously.
\item \textbf{Compute-value supervision}: targets derived offline from the
trained model itself---per-token loss, and $\partial\mathcal{L}/\partial g$
in one backward pass---match label-supervised allocation, with a
shuffled-targets control (and a pre-registered decision rule) separating
the specific measured targets from supervised variation alone
(\S\ref{sec:measure}, \S\ref{sec:p0}); the value field's per-bin signature
is measured at 42M and 150M, where the pre-registered migration prediction
fails and the converged field is flat (\S\ref{sec:scale}).
\item \textbf{Iso-FLOPs methodology}: all method comparisons at matched
\emph{billed training FLOPs} (gated billing, forward + backward), with
within-segment interpolation and a budget sweep; the workshop version's
``similar total budget'' caveat is retired (\S\ref{sec:isoflops}).
\item \textbf{Scale replication, honestly scoped}: the failure taxonomy,
the shuffled-control content-irrelevance result, and the causal
field-chasing test all replicate at 150M (probe grid, starved regime); the
dropout recipe's parity claim is scoped to compute-limited regimes by its
pre-registered decision rule ($-5.3\%$ loss at $50\%$ of billed FFN FLOPs
at 150M; $-7.8\%$ at 1.4B web-text fine-tuning; the boundary on
template-dominated data is reported, not hidden)
(\S\ref{sec:scale}, \S\ref{sec:nl}).
\item \textbf{Realization}: a tiered-FFN serving benchmark showing
tokens/sec scaling with the mean gate, converting billed-FLOPs savings into
wall-clock (\S\ref{sec:realization}).
\end{enumerate}

\section{Related Work}
\label{sec:related}

\paragraph{Adaptive compute.} Adaptive Computation Time
\citep{graves2016act}, early-exit and cascades, and Mixture-of-Depths
\citep{raposo2024mixture} route tokens by learned signals; MoE+router work
attaches learned routers to expert selection. \citet{raposo2024mixture} train
routers from scratch; our MoD baseline on the language-model track inherits
the resulting handicap---a router trained during a short fine-tune, not a
full pretraining run---which we acknowledge as a baseline limitation, not a
strawman.
\paragraph{Conditional computation and mixture-of-experts.}
\paragraph{Entropy and confidence as uncertainty proxies.} Draft-model
disagreement in speculative decoding is a deployed, per-token difficulty
signal: acceptance probability measures ``hard for a small model,'' and our
gate-gradient targets are the same quantity computed for layers instead of
models.
\paragraph{Resource-aware agents.}

\section{Method}
\label{sec:method}

\subsection{Architecture and per-token gating}
\label{sec:arch}

Each layer computes
\begin{equation}
x_2 = \LN\!\bigl(x_1 + \attn(x_1)\bigr), \qquad
x_3 = \LN\!\bigl(x_2 + g_t \cdot \FFN(x_2)\bigr),
\label{eq:gate}
\end{equation}
where $g_t \in [0,1]$ is a per-token, per-layer gate produced by a small
per-layer \emph{DifficultyHead} (a two-layer MLP on the post-attention state
$x_2$, sigmoid-squashed). Compute is billed proportionally: a position gated
at $g$ is billed $g$ times the per-token FFN cost; attention is always paid
in full. All variants share Eq.~\eqref{eq:gate} and the accounting, so
comparisons isolate the gating signal. We gate the FFN only, never the
attention's keys or values: skipping a token's FFN is safe because downstream
tokens still attend to it, whereas skipping K/V irreversibly deletes
information that later positions may need.

\subsection{Why endogenous signals fail: three diagnostics}
\label{sec:failures}

End-to-end training offers the gate one channel of feedback: the task loss.
\emph{Collapse}: a signal with no difficulty information (attention entropy,
$r \approx -0.06$ per token with difficulty) drives the gate to a constant.
\emph{Inversion}: a ground-truth initialization makes the gate vary, but easy
problems are memorizable, so their cross-entropy gradient is largest and
training allocates them \emph{more} compute (per-problem gate--difficulty
$r = -0.59$). \emph{Vacuity}: a head trained only through the task loss
receives no signal about what difficulty is; the gate stays flat and unstable
across seeds. We report the two iteration-1 quantities as distinct: per-token
entropy--difficulty correlation $r \approx -0.06$, and per-problem
entropy-gate--difficulty $r = 0.23$ (the gate passes the tag embedding through
a bottleneck; the two correlations measure different objects).

\subsection{Explicit difficulty supervision with a budget constraint}
\label{sec:supervised}

Training is two-stage. \textbf{Stage 1} trains the body conventionally with
the gate fixed at $1$ and checkpoints it. \textbf{Stage 2} attaches the
DifficultyHeads and minimizes
\begin{equation}
\mathcal{L} = \mathcal{L}_{\mathrm{CE}}
+ \lambda_{\mathrm{mse}}\,\tfrac{1}{N}\sum_{t}\bigl(\sigma(s_t) - \tau_t\bigr)^2
+ \lambda_{\mathrm{budget}}\,\bigl(\bar{g} - 0.5\bigr)^2,
\label{eq:loss}
\end{equation}
with $\lambda_{\mathrm{mse}} = 5.0$, $\lambda_{\mathrm{budget}} = 0.5$; the
body trains at a near-frozen learning rate ($10^{-2}\times$ the heads').
Stage 1 is shared by every fork of the method (all Stage-2 arms load the same
checkpoint per seed), so method comparisons are paired by body.

\subsection{Measuring the value of compute}
\label{sec:measure}

Where difficulty labels are absent (all natural-language settings), targets
$\tau_t$ are \emph{measured offline} from the Stage-1 checkpoint:
\begin{equation}
\mathrm{score}_t \;=\; -\sum_{\ell} \frac{\partial \mathcal{L}}{\partial
g_{t,\ell}}\Big|_{g=1},
\label{eq:gategrad}
\end{equation}
the marginal loss reduction from one unit of FFN compute at token $t$,
obtained for all tokens and layers in a single forward--backward pass with
gates as differentiable leaves. Scores ${\le}0$ are \emph{irreducible}
(compute does not help there) and receive the floor target $0.1$; positive
scores are quartile-binned to $0.2/0.4/0.6/0.8$. This is the paper's key
conceptual move: tags label \emph{difficulty}; Eq.~\eqref{eq:gategrad}
measures the \emph{value} of compute. The two are different objects, and the
difference is testable: score--loss correlation is near zero in our runs---%
loss says ``hard,'' the gate gradient says ``helpable''---and a token's tag
bin does not constrain its score (a hard bin mixes irreducible tokens with
worth-treating ones). We therefore expect, and report, that compute-value
supervision does \emph{not} order gates by difficulty bins at the testbed
scale: bin-level spread is not its target. The workshop-era inversion---%
end-to-end training overfeeding easy tokens---is the same phenomenon seen
from the other side: training chases reducibility wherever it lives, but
uncontrollably; the budget term is what makes chasing value safe. A
loss-quantile variant (the proxy-difficulty ablation) bins tokens by
per-token loss; a \emph{shuffled} control permutes targets within each
sequence, testing whether any varying supervision would do.

\paragraph{First-order oracle allocation.} The same measurements give a
reference point: sorting per-(token, layer) scores and filling until the
budget is exhausted yields the first-order-optimal allocation under a
\emph{frozen} body, evaluated zero-shot by overriding the gates at
inference. We label it accordingly: it bounds what allocation alone buys
without retraining. Trained methods may exceed it---their bodies adapt to
their gates---and we interpret that as adaptation compounding allocation,
not as a paradox.

\subsection{Iso-FLOPs training protocol}
\label{sec:isoflops}

Every run logs cumulative \emph{billed training FLOPs}: the gated billing of
\S\ref{sec:arch} applied to forward, with backward billed at twice forward
(total $3\times$). Methods are compared at matched achieved budgets by
linear interpolation of the evaluation history \emph{within} training
segments only---two-stage runs have a kink at the stage boundary, and budgets
common to all methods are capped at the shortest run's endpoint (no
extrapolation). At matched billed FLOPs, lower-mean-gate methods receive more
optimizer steps per billed dollar; that is the deployment claim under test,
and steps-matched comparisons are reported alongside in
Appendix~\ref{app:stepsmatched}.

\section{Experimental setup}
\label{sec:setup}

\paragraph{Arithmetic.} Two regimes with graded difficulty: \emph{tagged}
([E]/[M]/[H] prefixes; easy 1--2-digit $\pm$, medium 4--5-digit $\pm$, hard
$4{\times}4$, $5{\times}4$ and $6{\times}5$-digit multiplication at scale) and
\emph{mixed} (no tags; operands 1--7 digits; bin = larger operand's digit
count). The hard regime is chosen per scale so the largest model sits inside
a difficulty window: hard-bin accuracy neither saturated nor collapsed
(maintained in $[40\%, 85\%]$ by the calibration probes). Frozen held-out
sets, identical across methods and seeds; accuracy on answer tokens only.
Models: an 11M testbed (the workshop setting, exactly reproduced) and a
150M scaled model (RoPE, flash attention, bf16, cosine schedule; identical
across methods), probed healthy (10k steps) and starved (2.5k steps).

\paragraph{Natural language.} Pretrained Pythia-1.4b continue-trained on
web text (FineWeb-Edu, 350M packed tokens, sequence length 2048, AdamW at
$10^{-5}$ with cosine decay and 2\% warmup, bf16 autocast over fp32
master weights): a dense arm and a token-level-FFN-dropout arm, same data
and step budget, the recipe pair of \S\ref{sec:causal} at language scale.
The oracle machinery of \S\ref{sec:measure} and the code/math domains
(CodeSearchNet, MetaMathQA) are implemented but were not run at NL scale
and are not claimed. Metric: full-held-out loss; the rotation arm is
reported at dense deployment (gates off), its pre-registered evaluation
mode.

\paragraph{Methods compared.} \emph{Baseline} (dense; = Stage-1), ours
(\emph{gate-grad} targets; \emph{loss-quantile} ablation), \emph{shuffled}
control, \emph{Fixed-schedule} (static $g{=}0.5$), \emph{MoD} at matched
capacity, \emph{Chemical-off} (architecture control), and the endogenous
iterations (\emph{entropy}, \emph{random}, \emph{tag-only}) at testbed scale.

\section{Results}
\label{sec:results}

\subsection{Endogenous signals fail; supervision fixes allocation}
\label{sec:failures-results}

Table~\ref{tab:testbed} and Figure~\ref{fig:journey}: \XX. (Ported from the
workshop version: entropy $0.232/0.233/0.233$ across E/M/H; tag-init
inverted; unsupervised vacuous; supervised spread $+0.20$, ordering
$r = 0.95$ in every seed.)

\subsection{Measured value matches labels at the testbed}
\label{sec:p0}

\begin{table}[t]
\centering
\caption{P0: paired Stage-2 arms from the same Stage-1 body per seed
(3 seeds, mean$\pm$std). Billed training FLOPs are identical across the
bottom two arms by construction (the oracle pass is billed to both); the
tag arm has no oracle pass.}
\label{tab:p0}
\pzerotable
\end{table}

\paragraph{Pre-registered decision rule.} Written before any shuffled number
existed: the measured-targets claim survives iff the gate-grad arm beats the
shuffled control by ${\ge}1.0$ point of held-out accuracy, paired by seed,
at matched billed training FLOPs, across all three seeds. No post-hoc
reinterpretation either way.

\paragraph{Outcome: the rule fired against the measured targets.} Paired
gate-grad $-$ shuffled accuracy across seeds: $-0.003$, $-0.001$, $-0.000$
(Table~\ref{tab:p0}).
At testbed scale, \emph{any} varying supervision under the budget
constraint---even targets permuted into noise---recovers the same accuracy
($0.747 \pm 0.009$ shuffled vs.\ $0.745 \pm 0.008$ gate-grad vs.\ $0.748 \pm
0.010$ tag) and indistinguishable gate structure at this scale (the tag arm alone
orders, with a $+0.21$ spread that does not move accuracy), while all
three dominate a
constant gate trained from scratch by ${\sim}2$ points at the same inference
budget. Two honest readings follow. First, the workshop result sharpens: at
$11$M, what supervision supplies is not allocation quality but a dense-then-
gate protocol whose accuracy is insensitive to target content; allocation
structure (the tag arm's $+0.21$ spread) is real but not
accuracy-load-bearing here. Second, the value-of-compute concept is not
refuted---it is falsifiable at scale: if Eq.~\eqref{eq:gategrad} measures
reducible value rather than noise, its per-bin signature must migrate as
model capability grows (\S\ref{sec:scale}), and under it the natural-language
targets differ from shuffled ones precisely where compute has leverage
(\S\ref{sec:nl}). Both tests are pre-registered; \XX\ reports their outcome.

\begin{table}[t]
\centering
\caption{Supervision targets vs.\ realized gates (budget redistribution).
The floor + quartile targets have mean $\approx 0.31$ and nominal spread
$0.7$; the pinned budget ($\bar g \to 0.5$) lifts and compresses them. The
compression is the budget term redistributing, not the head failing to
allocate --- a pinned mean is what makes the inference budget a settable
dial.}
\label{tab:budget}
\budgettable
\end{table}

Bin-level spread is \emph{not} the allocation metric for measured targets
(\S\ref{sec:measure}): gate-grad varies within problems (a hard bin contains
both irreducible and worth-treating tokens), so its E/M/H spread is smaller
while its per-token allocation is finer; we report gate--score agreement on
held-out tokens (\XX) and per-token rather than per-bin summaries.

\subsection{The causal test and the surviving recipe}
\label{sec:causal}

\begin{table}[t]
\centering
\caption{11M causal single-stage test: the gate is live from step 0 at
budget 0.5, all arms at matched billed training FLOPs (seed 0). Dropout and
random windows are indistinguishable; chasing the measured field online is
far worse than random; a constant gate collapses.}
\label{tab:causal}
\causaltable
\end{table}

Table~\ref{tab:causal}: token-level FFN dropout and random 100-step windows
are indistinguishable ($1.275$ vs $1.274$ held loss)---the mechanism is the
noise-regularization of intermittent full compute, not any allocation
signal. Online field-chasing against the measured value field is much worse
than random ($2.253$ vs $1.274$), and a constant gate collapses ($2.621$).
Dense wins dense-inference at this horizon ($1.167$); the question is what
half-FLOP training buys at convergence.

\paragraph{The 150M convergence pair.} At 10k steps, the rotation-trained
model holds held-out loss $\ConvRotLoss$ vs dense $\ConvDenseLoss$
(${+}\ConvRotGapPct\%$) at exactly \ConvRotFlopsPct\% of the billed
training FLOPs. The pre-registered rule (within ${\sim}2\%$ of dense:
parity advances; $2$--$10\%$: scoped) puts this in the scoped band: the
claim is compute-limited-regime, stated plainly.

\begin{table}[t]
\centering
\caption{150M probe grid: 6 arms $\times$ 2 seeds at matched budget
(mean$\pm$std over seeds). Dense wins the healthy recipe; every gated
2-stage arm ties regardless of target content; MoD ties dense on mean
accuracy with an \emph{anti-ordered} router (hard bins receive the least
compute).}
\label{tab:p150}
\pgridtable
\end{table}

Table~\ref{tab:p150}: content-irrelevance replicates at 150M---tag,
shuffled, and fixed 2-stage arms are accuracy-identical ($62.9$ each) and
dense wins the healthy recipe ($68.0$). MoD's trained router inverts
(per-bin gates $0.25/0.24/0.21$, spread $-0.05$) while tying dense on mean
accuracy ($67.5$); seed-paired diffs ($+8.9/{-}9.7$) make that a wash, not
a win.

\begin{table}[t]
\centering
\caption{Starved-150M causal test (the field is structured mid-life here:
permutation-controlled waterfill beats random by $+0.30/+0.38$ across
seeds). Yet field-chasing still loses to random windows, and a static gate
collapses outright. Measurable structure is not a trainable signal.}
\label{tab:starved}
\starvedtable
\end{table}

Table~\ref{tab:starved} is the decisive test. The starved-150M regime is
where the value field is demonstrably \emph{alive}: the discriminator shows
real ordering structure (waterfill $+$0.30/$+$0.38 over random across two
seeds). If that structure were exploitable, training online against the
measured field should beat random allocation. It does not: online loses to
random windows ($41.7$ vs $44.2$), replicating the 11M causal result at a
second scale, and the static gate collapses outright ($22.4$, held loss
diverging). The life cycle and the causal test together: the field's
structure is real, measurable---and not a training signal.

\subsection{Scale: the effect survives 11M $\to$ 150M}
\label{sec:scale}

\paragraph{Pre-registered readout: value migrates with capability.} If the
oracle measures value rather than difficulty, its per-bin signature must
change with model scale: an 11M model finds most hard-bin tokens
irreducible, but a $400$M model does not. Prediction (written before the P1
probes ran): per-bin mean gate-grad score on the hard bin rises, and its
fraction of non-positive scores falls toward the medium bin's, across
$42$M $\to$ $150$M $\to$ $400$M. A flat signature at \$10 of probe cost
would have falsified the irreducibility mechanism before the expensive
stage.

\emph{The rule fired against the prediction.} On healthy bodies trained
with the validated recipe (42M acc $0.496$, 150M acc $0.563$), the measured
field is flat at convergence everywhere: frac(score${\le}0)\approx 0.51$
across E/M/H bins at both sizes, mean $|$score$|$ at $10^{-6}$ scale
(coin-flip; Table~\ref{tab:migration}). The first-order marginal value of
FFN compute on a converged body is uninformative---no difficulty-bin
structure, no migration. The compute-value concept is therefore parked as
a \emph{training-time} signal; what survives is its mid-life measurability
(\S\ref{sec:causal}): the starved-regime discriminator shows the field is
structured where the body is still plastic, even though neither oracle
allocation after convergence nor online chasing during training converts
that structure into accuracy.

\begin{table}[t]
\centering
\caption{Per-bin measured value signature by model size (converged bodies,
validated recipe). frac(score${\le}0)\approx 0.5$ and mean scores at
$10^{-6}$: the converged field is coin-flip at every bin and both scales
(pre-registered readout executed; prediction not confirmed).}
\label{tab:migration}
\migrationtable
\end{table}

Figure~\ref{fig:frontier-scale}: accuracy vs.\ billed inference FLOPs at
matched training budgets across \todoX{11M/150M/400M/1B}. \XX

\subsection{Natural language: the recipe transfers, with a boundary}
\label{sec:nl}

\begin{table}[t]
\centering
\caption{NL transfer: dense vs.\ token-level FFN dropout at seed 0 per
domain (Pythia-1.4b, 3000 steps, matched data; rotation evaluated at dense
deployment). Web text sits inside the pre-registered scoped band; code is
marginal; on template-dominated math the recipe fails outright --- its
noise-regularization directly opposes the memorization that low loss there
is made of.}
\label{tab:nl}
\nltab
\end{table}

Table~\ref{tab:nl}: Pythia-1.4b continue-trained on web text, Python code,
and chain-of-thought math (350M packed tokens where available, 3000 steps,
seed 0; both arms share tokenizer, data, and the accounting of
\S\ref{sec:isoflops}). Three outcomes, reported plainly. \emph{Web text}:
dense \NlWebtextDense{} vs.\ rotation-at-dense-deployment
$\NlRotLoss$ = ${+}\NlRotGapPct\%$ at exactly half billed training FLOPs
--- inside the same pre-registered $2$--$10\%$ band as the 150M testbed
($+5.3\%$). \emph{Code}: +\NlCodeGap\% --- just outside the band;
marginal, one seed. \emph{Math}: +\NlMathGap\% --- the recipe fails.
The mechanism is legible: MetaMathQA held-out loss is dominated by
memorizable template structure (dense reaches $0.382$), and intermittent
full compute is precisely a regularizer against that --- the recipe buys
its robustness by selling memorization. Evaluated \emph{with the ablation
live} the rotation models are far worse (web text $3.378$), which is the
point of the deployment claim: the trained body wants full compute at
inference, and gets it. The scoping this buys is stated rather than hidden:
the half-cost recipe is for capability-limited regimes on
distributed-structure data, not for redundancy-dominated corpora.
\emph{The taxonomy row transfers.} Forking the web-text Stage-1 body into
supervised stage-2 arms (\S\ref{sec:supervised}): gate-grad targets reach
$2.7077$, shuffled targets $2.7085$ --- a paired difference of $0.0008$,
against the $1.0$-point pre-registered threshold --- with flat gate
profiles in both. On language, measured value orders nothing and buys
nothing over permuted targets: the content-irrelevance result is not a
testbed artifact. Ablations (App.~\ref{app:ablations}): full-unfreeze
stage-2 recovers $2.6054$ of the gap --- bodies adapt to their gates ---
and per-layer targets match summed ones. Negative-space honesty: the
oracle machinery's difficulty diagnostics are testbed-scale results
(\S\ref{sec:failures-results}, \S\ref{sec:causal}).

\subsection{Where does the compute go? Oracle gap and layer profile}
\label{sec:oracle-gap}

Against the first-order frozen-body oracle (\S\ref{sec:measure}): \XX.
Per-layer gate profiles by difficulty bin: \XX.

\subsection{Wall-clock realization}
\label{sec:realization}

Billed FLOPs are an accounting device; this section measures what a
realization actually pays. We time full training steps (forward + backward,
$4{\times}2048$ tokens) of Pythia-1.4b under three execution strategies:
dense; the \emph{masked} path our training driver uses (compute every FFN
row, multiply masked tokens by zero); and a \emph{gather-skip} path that
removes masked tokens' FFN rows (index-select on the flattened token
axis). On an A100: dense runs at $\WcDenseTok$ tok/s; masked-at-half runs
at $\WcMaskedHalf\times$ --- zeroing saves nothing --- while gather-skip
reaches $\WcSkipHalf\times$ dense at budget $0.5$, $\WcSkipThreeQ\times$
at $0.75$, and $\WcSkipQuarter\times$ at $0.25$. Two honest readings.
First, the saving is real but sub-linear: FFNs are only a fraction of step
time (attention, optimizer, and memory movement do not shrink), so the
billed $2\times$ arrives as a measured $1.35\times$. Second, the naive
masked path --- what most token-dropping code actually ships --- realizes
\emph{nothing}; the gather is the whole result.

\section{Discussion}
\label{sec:discussion}

\paragraph{Implications.} The recipe---train dense, measure, impose---needs
no endogenous-signal search and plugs into serving stacks that already
produce per-token difficulty estimates (speculative-decoding acceptance).
\paragraph{When static schedules suffice.} \XX
\paragraph{Limitations.} The 1.4B web-text headline rests on two seeds
(a third was attempted three times and lost to host failures twice);
they agree to $0.1$ points, but $n{=}2$ is $n{=}2$. The code and math
transfers are single-seed. Arithmetic difficulty is operand-size-defined at
testbed scale; the natural-language bins inherit the reference model's
notion of difficulty. The two-stage body is near-frozen by default; a
full-unfreeze ablation is reported in Appendix~\ref{app:ablations}. Billed
FLOPs are an accounting device; \S\ref{sec:realization} is the bridge to
wall-clock, and realized speedups depend on kernel support. Gate-target
quartiles are hand-set; Eq.~\eqref{eq:gategrad} replaces their content but
not their form.

\section*{Reproducibility statement}
All code, manifests, and per-run JSONs (config, history, billed-FLOPs
curves) are in the anonymized repository; every table and figure regenerates
from committed artifacts. Seeds, held-out sets, and the FLOPs accounting are
specified in \S\ref{sec:setup} and Appendix~\ref{app:details}.

\section*{Compute statement}
Total rented-GPU spend for every result in this paper: approximately
\$30 (itemized per stage in Appendix~\ref{app:compute}); testbed runs on
an Apple M3 (MPS). Roughly half was spent on results and half on re-runs
after host failures and fixable training bugs --- we report both, because
the failure half is what the manifests and kill-rules in the repository
are for.

\bibliography{iclr2027_conference}
\bibliographystyle{iclr_conference}

\appendix

\section{Steps-matched comparisons}
\label{app:stepsmatched}
\XX

\section{Ablations}
\label{app:ablations}

\paragraph{Full-unfreeze Stage-2.} The default protocol near-freezes the
body ($10^{-2}\times$ the head learning rate). Unfreezing it fully on the
web-text fork (seed 1, 2000 steps): held loss $2.6054$ vs.\ the
near-frozen $2.7077$ and dense $2.5322$ --- the body adapting to its
gates recovers roughly $40\%$ of the remaining gap, consistent with the
main-text reading that trained methods can exceed frozen-body allocation
by adaptation, not by better ordering (gate profiles stay flat:
$[0.33, 0.34, 0.35, 0.35]$).
\paragraph{Per-layer target freedom.} \XX
\paragraph{Shuffled control at all scales.} 11M: Table~\ref{tab:p0};
150M: Table~\ref{tab:p150}; 1.4B web text: \S\ref{sec:nl} --- the paired
gate-grad $-$ shuffled difference is $0.0008$ at language scale,
replicating the testbed result.

\section{Experimental details}
\label{app:details}
\XX

\section{Compute ledger}
\label{app:compute}
\XX

\end{document}
