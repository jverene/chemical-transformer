[project]
name = "chemical-transformer"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.5",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "scipy>=1.11",
]

[project.optional-dependencies]
nl = ["transformers>=4.40", "datasets>=2.19", "accelerate>=0.30"]
